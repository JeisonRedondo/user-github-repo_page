import styled from "styled-components";

const StyledLabel = styled.label`
  width: fit-content;
  height: fit-content;
`;

function Label() {
  return <StyledLabel>Label de prueba</StyledLabel>;
}

export default Label;
